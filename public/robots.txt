User-agent: *
Allow: /

# Admin panel and its API — nothing here is for search engines, and the
# panel is password-gated anyway.
Disallow: /admin
Disallow: /admin/

# The two doors into the product. Both are sign-in forms with no content of
# their own, and every other app route now redirects a logged-out visitor to
# the landing page — so these are the only thin pages a crawler can reach.
Disallow: /start
Disallow: /login

Sitemap: https://jashmenstudio.com/sitemap.xml
